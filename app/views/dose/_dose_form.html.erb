<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.order(:name) %>

  <%= f.submit "Add dose", class: "btn text-white", style:"background-color: #6ea4f7;" %>
<% end %>
