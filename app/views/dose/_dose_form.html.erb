<%= simple_form_for [@cocktail, @dose], class: 'form-inline' do |f| %>

  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.order(:name) %>

  <%= f.submit "Add dose", class: "btn button" %>
<% end %>
